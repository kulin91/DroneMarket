import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  containerForItem: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 260,
    marginTop: 21,
  },
  viewScrollItem: {
    height: '100%',
  },
});